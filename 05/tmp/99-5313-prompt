<Context>
You receive a selection in neovim that you need to replace with new code.
The selection's contents may contain notes, incorporate the notes every time if there are some.
consider the context of the selection and what you are suppose to be implementing
<SELECTION_LOCATION>
range(point(19,1),point(36,46))
</SELECTION_LOCATION>
<SELECTION_CONTENT>
class NumericProcessor(DataProcessor):
    def process(self, data: Any) -> str:
        try:
            if isinstance(data, (list, tuple)):
                numbers = [float(x) for x in data if isinstance(x, (int, float))]
                if numbers:
                    return f"sum={sum(numbers)}, avg={sum(numbers)/len(numbers)}"
            return str(data)
        except (ValueError, TypeError, OverflowError) as e:
            return f"Error processing data: {e}"

    def validate(self, data: list | tuple | int | float) -> bool:
        if isinstance(data, (list, tuple)):
            for x in data:
                if isinstance(x, (int, float)):
                    return True
            return False
        return isinstance(data, (int, float))
</SELECTION_CONTENT>
<FILE_CONTAINING_SELECTION>
#!/usr/bin/env python3

from typing import Any

from ABC import abc, abstractmethod


class DataProcessor(ABC):
    @abstractmethod
    def process(self, data: Any) -> str: ...

    @abstractmethod
    def validate(self, data: Any) -> bool: ...

    def format_output(self, result: str) -> str:
        return result


class NumericProcessor(DataProcessor):
    def process(self, data: Any) -> str:
        try:
            if isinstance(data, (list, tuple)):
                numbers = [float(x) for x in data if isinstance(x, (int, float))]
                if numbers:
                    return f"sum={sum(numbers)}, avg={sum(numbers)/len(numbers)}"
            return str(data)
        except (ValueError, TypeError, OverflowError) as e:
            return f"Error processing data: {e}"

    def validate(self, data: list | tuple | int | float) -> bool:
        if isinstance(data, (list, tuple)):
            for x in data:
                if isinstance(x, (int, float)):
                    return True
            return False
        return isinstance(data, (int, float))


class TextProcessor(DataProcessor):
    pass


class LogProcessor(DataProcessor):
    pass


def main() -> None:
    pass


if __name__ == "__main__":
    main()
</FILE_CONTAINING_SELECTION>

</Context>
<Prompt>
don't use any since it's not a good practice here.
</Prompt>

<Location><File>/home/aksel/project/school/python_modules/05/ex0/stream_processor.py</File><Function>range(point(19,1),point(36,46))</Function></Location>
<FunctionText>class NumericProcessor(DataProcessor):
    def process(self, data: Any) -> str:
        try:
            if isinstance(data, (list, tuple)):
                numbers = [float(x) for x in data if isinstance(x, (int, float))]
                if numbers:
                    return f"sum={sum(numbers)}, avg={sum(numbers)/len(numbers)}"
            return str(data)
        except (ValueError, TypeError, OverflowError) as e:
            return f"Error processing data: {e}"

    def validate(self, data: list | tuple | int | float) -> bool:
        if isinstance(data, (list, tuple)):
            for x in data:
                if isinstance(x, (int, float)):
                    return True
            return False
        return isinstance(data, (int, float))</FunctionText>
<TEMP_FILE>./tmp/99-5313</TEMP_FILE>
<MustObey>
NEVER alter any file other than TEMP_FILE.
never provide the requested changes as conversational output. Return only the code.
ONLY provide requested changes by writing the change to TEMP_FILE

never attempt to read TEMP_FILE.
It is purely for output.
Previous contents, which may not exist, can be written over without worry
After writing TEMP_FILE once you should be done.  Be done and end the session.

</MustObey>